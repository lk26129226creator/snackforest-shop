# snackforest-shop

簡易 Spring Boot 範例專案。

快速上手

建置：
```bash
mvn clean package
```

啟動（開發）：
```bash
mvn spring-boot:run
```

或執行產出的 jar：
```bash
java -jar target/snackforest-shop-0.0.1-SNAPSHOT.jar
```

範例 API：
- GET /api/hello → 回傳 JSON {"message":"Hello from snackforest-shop"}
